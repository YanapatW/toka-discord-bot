---
name: typecheck
description: /typecheck — Run TypeScript type checking
---

# Type Check

Run TypeScript compiler in check-only mode.

## Steps
1. Run: `pnpm exec tsc --noEmit`
2. Report any errors found
3. If errors exist, suggest fixes
